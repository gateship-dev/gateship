node_modules/
dist/
out/
*.tgz
.*.bun-build

coverage/
*.lcov
gate-results.json

.env
.env.*.local
*.log
*.tsbuildinfo
.cache/

.idea/
.cursor/
.DS_Store
Refactor*.md

# The UI bundle is a build output, produced by the `prepare` script on install
# and by `build:release`. It is never committed: a committed copy can only ever
# be older than webui/src.
webui/dist/

# Local runtime and test state.
.gship/
.local/
.claude/
.gship-test-tmp/
test-results/

# Optional, local-only Claude Code source reference.
claude-code-harness/
